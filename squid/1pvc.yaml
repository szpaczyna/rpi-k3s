apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: squid-pvc
  namespace: proxy
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
