kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "name" . }}-pvc
  labels:
    type: local
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
