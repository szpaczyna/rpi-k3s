---
# DEPRECATED: Consolidated into lint-all.yml
name: (Deprecated) Legacy JSON/YAML Linter
permissions:
  contents: read

on:
  workflow_dispatch: {}

jobs:
  json-yaml-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Validate JSON and YAML files
        uses: GrantBirki/json-yaml-validate@v4.0.0
        with:
          mode: warn
          exclude_file: .github/validate-ignore.txt
