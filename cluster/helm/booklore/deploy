#!/bin/bash
set -euo pipefail

NAMESPACE="media"
RELEASE_NAME="booklore"

kubectl apply -f <(sops -d ./db-secret.enc.yaml) -n "${NAMESPACE}"

helm upgrade --install "${RELEASE_NAME}" . \
  --namespace "${NAMESPACE}" \
  --create-namespace \
  --values values.yaml
