#!/bin/bash
set -euo pipefail

NAMESPACE="monitoring"
RELEASE_NAME="x509-certificate-exporter"

helm upgrade --install "${RELEASE_NAME}" . \
  --namespace "${NAMESPACE}" \
  --create-namespace \
  --values values.yaml
