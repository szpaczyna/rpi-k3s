#!/bin/bash
set -euo pipefail

NAMESPACE="monitoring"
RELEASE_NAME="event-exporter"

helm upgrade --install "${RELEASE_NAME}" . \
  --namespace "${NAMESPACE}" \
  --create-namespace \
  --values values.yaml
