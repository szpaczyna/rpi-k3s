---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gentoo
  namespace: apps
  labels:
    app.kubernetes.io/name: gentoo
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gentoo
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gentoo
    spec:
      containers:
      - name: gentoo
        image: ksmanis/gentoo-distcc:tcp
        ports:
        - containerPort: 3632
          name: distcc-tcp
        volumeMounts:
          - mountPath: /mnt
            name: data-gentoo
            readOnly: false
        resources:
          requests:
            cpu: 0.1
            memory: 128Mi
          limits:
            cpu: 3
            memory: 2048Mi
      volumes:
        - name: data-gentoo
          persistentVolumeClaim:
            claimName: data-gentoo
      nodeSelector:
        kubernetes.io/role: worker
