---
## calibre-web
apiVersion: apps/v1
kind: Deployment
metadata:
  name: calibre-web
  labels:
    app.kubernetes.io/name: calibre-web
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: calibre-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: calibre-web
    spec:
      serviceAccountName: media
      dnsPolicy: ClusterFirst
      enableServiceLinks: true
      securityContext: {}
        #runAsGroup: 1001
        #runAsUser: 1001
      containers:
        - name: calibre-web
          #image: "szpaczyn/calibre-web-arm64:v0.6.12"
          image: "lscr.io/linuxserver/calibre-web:0.6.14"
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: "Europe/Warsaw"
            - name: PUID
              value: "1001"
            - name: PGID
              value: "1001"
            - name: UMASK
              value: "22"
          ports:
            - name: calibre-web
              containerPort: 8083
              protocol: TCP
          volumeMounts:
            - mountPath: /config
              name: media
              subPath: library/books
            - mountPath: /books
              name: media
              subPath: library/books
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
      volumes:
      - name: media
        persistentVolumeClaim:
          claimName: media
      nodeSelector:
        kubernetes.io/role: worker
