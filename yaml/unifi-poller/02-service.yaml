---
apiVersion: v1
kind: Service
metadata:
  name: unifi-poller
  labels:
    app.kubernetes.io/name: unifi-poller
spec:
  type: ClusterIP
  ports:
    - port: 9130
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: unifi-poller
