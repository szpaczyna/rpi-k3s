---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elk-data-01
  labels:
    app: elk
    name: elk
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "12Gi"
  storageClassName: longhorn
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: elk-data-02
  labels:
    app: elk
    name: elk
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "3Gi"
  storageClassName: longhorn
