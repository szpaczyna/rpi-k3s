---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: nextcloud
  namespace: cloud
spec:
  scaleTargetRef:
    kind: Deployment
    name: nextcloud
    apiVersion: apps/v1
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 150
