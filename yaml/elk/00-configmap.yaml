apiVersion: v1
kind: ConfigMap
metadata:
  name: kibana-config
  labels:
    name: elk
data:
  kibana.yml: |
    ---
    server.name: kibana
    server.host: "0.0.0.0"
    elasticsearch.hosts: [ "http://elk-svc-elasticsearch.test.svc.cluster.local:9200" ]
    elasticsearch.ssl.verificationMode: none
    status.allowAnonymous: true
