apiVersion: v1
kind: Service
metadata:
  labels:
    app: pihole-exporter
  name: pihole-exporter
  namespace: monitoring
spec:
  ports:
  - name: http
    port: 9617
    targetPort: http
    protocol: TCP
  selector:
    app: pihole-exporter
