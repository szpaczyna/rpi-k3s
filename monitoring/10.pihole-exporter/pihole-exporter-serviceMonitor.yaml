apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app: pihole-exporter
  name: pihole-exporter
  namespace: monitoring
spec:
  endpoints:
  - interval: 180s
    port: http
  selector:
    matchLabels:
      app: pihole-exporter
