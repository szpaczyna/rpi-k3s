apiVersion: v1
kind: Service
metadata:
  labels:
    app: cert-manager
  name: cert-manager-prometheus-discovery
  namespace: cert-manager
spec:
  clusterIP: None
  ports:
  - name: metrics
    port: 9402
    targetPort: 9402
  selector:
    app: cert-manager
