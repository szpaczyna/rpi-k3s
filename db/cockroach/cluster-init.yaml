apiVersion: batch/v1
kind: Job
metadata:
  name: cluster-init
  labels:
    app: cockroachdb
spec:
  template:
    spec:
      containers:
      - name: cluster-init
        image: registry.shpaq.org/cockroachdb:v20.2.3 
        imagePullPolicy: IfNotPresent
        command:
          - "/cockroach/cockroach"
          - "init"
          - "--host=cockroachdb-0.cockroachdb"
      restartPolicy: OnFailure
